# GPG Agent configuration
enable-ssh-support
default-cache-ttl 60
max-cache-ttl 120
# pinentry-smart: forward the PIN prompt to nimbus's native pinentry-mac over a
# reverse-forwarded socket when nimbus is connected, else fall back to curses (#204)
# see smartsocket repo for source and installation instructions
pinentry-program pinentry-smart
